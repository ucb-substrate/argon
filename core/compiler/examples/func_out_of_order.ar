fn triple(x: Float) -> Float {
    x + double(x)
}

fn double(x: Float) -> Float {
    2. * x
}

cell test() {
    let shape = rect(Layer::Met1)!;
    eq(shape.x0, 0.);
    eq(shape.y0, 0.);
    eq(shape.y1, 100.);
    eq(shape.x1, triple(shape.y1));
}
