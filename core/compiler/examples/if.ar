cell if_test() {
  let x0 = 1.0;
  let y0 = float();
  let z = float();
  let w = if y0 < 2.0 {
    eq(z, 5.0);
    let tmp = if y0 < 2.0 { x0 } else { 2.0 };
    1.0
  } else {
    eq(z, 3.0);
    2.0
  };
  eq(x0, y0);
}
